{
  "version": 2,
  "builds": [
    { "src": "app/api/server.ts", "use": "@vercel/node" },
    { "src": "package.json", "use": "@vercel/next" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "app/api/server.ts" },
    { "src": "/(.*)", "dest": "/$1" }
  ]
}